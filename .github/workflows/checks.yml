###############################################################################
# WARNING: THIS FILE IS GENERATED!
#
# This file is generated by running the following command in the root directory
# of this Git repository:
#
#   python workflows/checks/generate.py >.github/workflows/checks.yml
#
# Changes made directly to this file will be clobbered when it gets
# regenerated. Instead, modify the template files that are used to generate
# this file.
###############################################################################

name: checks

on:
  push:
    branches: ['**']

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:

  python-format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install pyink

      - uses: actions/checkout@v3

      - run: ./scripts/format.sh -c

  python-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install pyflakes

      - uses: actions/checkout@v3

      - run: ./scripts/lint.sh

  python-test:
    strategy:
      fail-fast: false
      matrix:
        runs_on: ['ubuntu-latest', 'macos-latest', 'windows-latest']

    runs-on: ${{ matrix.runs_on }}
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install absl-py

      - uses: actions/checkout@v3

      - run: ./scripts/test.sh
        shell: bash

  generated_checks_yml:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install absl-py Jinja2

      - uses: actions/checkout@v3

      - run: python workflows/checks/generate.py >.github/workflows/checks.yml
      - run: git diff --exit-code

  cpp-test-linux-debug-gcc:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y ninja-build
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
        env:
          CC: gcc
          CXX: g++

  cpp-test-linux-release-gcc:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y ninja-build
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
        env:
          CC: gcc
          CXX: g++

  cpp-test-linux-debug-clang:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y ninja-build
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
        env:
          CC: clang
          CXX: clang++

  cpp-test-linux-release-clang:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install -y ninja-build
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
        env:
          CC: clang
          CXX: clang++

  cpp-test-macos-debug:
    runs-on: macos-latest
    steps:
      - run: brew install ninja
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

  cpp-test-macos-release:
    runs-on: macos-latest
    steps:
      - run: brew install ninja
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

  cpp-test-windows-debug-win32:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -G "Visual Studio 17 2022" -A Win32 -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

  cpp-test-windows-release-win32:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -G "Visual Studio 17 2022" -A Win32 -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

  cpp-test-windows-debug-x64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

  cpp-test-windows-release-x64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context



  cpp-test:
    steps:
      - if: runner.os == 'Linux'
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_config }} -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
        env:
          CC: ${{ matrix.compiler == 'clang' && 'clang' || 'gcc' }}
          CXX: ${{ matrix.compiler == 'clang' && 'clang++' || 'g++' }}
      - if: runner.os == 'macOS'
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=${{ matrix.build_config }} -G Ninja -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context
      - if: runner.os == 'Windows'
        run: cmake -S . -B build -G "Visual Studio 17 2022" -A ${{ matrix.target_architecture }} -DCONCPP_LOG_CMAKE_INFO=YES -DCMAKE_COMPILE_WARNING_AS_ERROR=YES --log-context

      - if: runner.os != 'Windows'
        run: cmake --build build
      - if: runner.os == 'Windows'
        run: cmake --build build --config ${{ matrix.build_config }}

      - if: runner.os != 'Windows'
        run: ctest --verbose
        working-directory: build
      - if: runner.os == 'Windows'
        run: ctest -C ${{ matrix.build_config }} --verbose
        working-directory: build
